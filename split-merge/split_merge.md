# 스플릿 머지 정렬

## 정렬 방법

길이가 $N$인 수열 $A=[A_1,A_2,⋯,A_N]$에 대하여, $A$가 정렬될 때까지 다음 과정 $X$를 반복한다.

1. 과정 $X$의 실행 횟수가 최소화되는 양의 정수 $x$를 구한다.
2. 수열 $A$의 원소 중 값이 $x$ 이하인 원소들을 원래 순서대로 추출하여 부분 수열 $B$를 만든다.
3. 수열 $A$의 원소 중 값이 $x$ 초과인 원소들을 원래 순서대로 추출하여 부분 수열 $C$를 만든다.
4. 기존 수열 $A$를, $B$와 $C$를 순서대로 이어 붙인 수열($B+C$)로 대체한다.

## 심화 응용

길이가 $N$인 수열 $A=[A_1,A_2,⋯,A_N]$가 있다.

1. 정렬하기 위해 필요한 모든 양의 정수 $x$의 값의 배열 $P=[x_1,x_2,⋯,x_k]$를 구한다.
2. 배열 $A$를 순회하면서 각 값을 $(-\infty,x_1], (x_1,x_2],⋯,(x_k,\infty)$ 중 하나로 분류하고 해당하는 배열(버킷)에 넣는다.
3. $(-\infty,x_1], (x_1,x_2],⋯,(x_k,\infty)$ 배열(버킷)을 모두 합친 배열 $B$를 만든다.

이 때 합친 배열 $B$는 $A$를 안정 정렬한 배열과 동일함을 증명할 수 있다.

## 코드 설명

- `get_x_count.py` `get_x_count.cpp` - 과정 $X$의 최소 실행 횟수를 구하는 코드이다.
- `get_x_list.py` `get_x_list.cpp` - 각각의 과정 $X$에서의 양의 정수 $x$의 값의 배열을 구하는 코드이다.
- `get_x_list_extended.py` `get_x_list_extended.cpp` - 수열 $A$의 일부 원소가 $N$보다 크거나 $1$보다 작은 경우에도 $x$의 값을 구하는 코드이다.
- `get_bucket_set.py` `get_bucket_set.cpp` - 수열의 각 값이 어떤 버킷에 해당하는지 전처리한 값을 구하는 코드이다.

## 참고

- `C++` 코드는 모두 $-10^9 < A_i < 10^9$일 때에만 작동한다.

## 참고 자료

- [KOI 2026 1차대회 2교시 초등부 3번 / 중등부 2번 문제 - 수열 정렬하기](https://www.biko.kr/problem/5936)
- [수열 정렬하기 - 모범 코드](https://assets.koi.or.kr/koi/2026/1/solutions/2/sorting.zip)